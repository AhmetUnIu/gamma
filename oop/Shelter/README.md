Animal class stores the animal’s name, species, and shelter cost.
Species class defines the animal types (cat, dog).
HowMuch() method calculates the shelter cost:
Cat: base shelter cost only
Dog: base shelter cost + walking fee (50 TL)
In Main, several animal objects are created and their costs printed to the console.
